pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Checkout the repository
                checkout scm

                // Build the Maven project
                echo "hello wolrd"
                //sh 'mvn clean package'
            }
        }
    }
}
